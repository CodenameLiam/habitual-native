import styled from '@emotion/native';

export const AllTrendContainer = styled.View`
    padding: 15px;
    padding-top: 10px;
    flex-direction: row;
    flex-wrap: wrap;
    justify-content: space-between;
`;
