import React from 'react';
// import { Picker } from '@react-native-picker/picker';

// const MAX_HOURS = 99;
// const MAX_MINUTES = 59;

// const hoursUnit = 'h';
// const minutesUnit = 'm';

// const getLabel = (i: number, unit: string | undefined): string => {
//     const numString = i.toString();
//     return `${numString} ${unit ?? ''}`;
// };

// export const getHoursItems = (colour: string): JSX.Element[] => {
//     const items = [];
//     for (let i = 0; i <= MAX_HOURS; i++) {
//         items.push(<Picker.Item key={i} value={i} label={getLabel(i, hoursUnit)} color={colour} />);
//     }
//     return items;
// };

// export const getMinutesItems = (colour: string): JSX.Element[] => {
//     const items = [];
//     for (let i = 0; i <= MAX_MINUTES; i++) {
//         items.push(<Picker.Item key={i} value={i} label={getLabel(i, minutesUnit)} color={colour} />);
//     }
//     return items;
// };
